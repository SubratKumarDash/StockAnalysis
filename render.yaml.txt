services:
  - type: web
    name: ai-stock-agent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn SSSAgent:app --host 0.0.0.0 --port 10000
    plan: free